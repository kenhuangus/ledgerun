node_modules
.next
out
build
dist

# env
.env
.env.local
.env*.local

# vendored reference API: keep the source, drop its git history
reference-api/.git

# logs / runtime
*.log
.drop/
uploads/

# generated email fixtures (regenerate with `npm run make:emails`)
sample-emails/

# demo video intermediates (slides/frames/audio — the .mp4 is committed)
demo/work/

# prisma
prisma/*.db

# os / editor
.DS_Store
*.tsbuildinfo
next-env.d.ts
